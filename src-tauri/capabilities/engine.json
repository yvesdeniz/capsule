{
  "identifier": "engine-remote",
  "description": "Narrow IPC surface for the hidden music.apple.com engine webview. Deliberately excludes every player/auth command the engine reports upward, it never drives the app.",
  "windows": ["engine"],
  "webviews": ["engine"],
  "remote": {
    "urls": ["https://music.apple.com", "https://*.apple.com"]
  },
  "permissions": [
    "core:default",
    "allow-engine-ready",
    "allow-engine-tokens",
    "allow-engine-event",
    "allow-engine-log"
  ]
}
